#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        string s;
        cin >> s;
        long long n;
        n = stoll(s);
        cout << n;
        int c = s.size() - 1;
        for (int i = 0; i < s.size() - 1; i++) {
            s[c] = '0'

        }
    }
}