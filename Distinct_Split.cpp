#include <bits/stdc++.h>

using namespace std;

int main () {
    int t;
    cin >> t;
    while (t--) {
        int n, mid;
        cin >> n;
        string s;
        cin >> s;
        set <string> set1, set2;
        if (n % 2 == 0)
        mid = n / 2;
        else
        mid = (n / 2) + 1;
        for (int i = 0; i < mid; ++i) {
            char c = s[i];
            
        }
    }
}