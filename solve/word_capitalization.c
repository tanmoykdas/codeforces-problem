#include<stdio.h>
#include<ctype.h>

int main()
{
    char a[1001],i,b,c;
    scanf("%s",&a);
     if(b=islower(a[0]))
        {if(b=1)
        a[0]=toupper(a[0]);
        }
    printf("%s",a);
}